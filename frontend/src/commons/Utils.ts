export default class Utils {
    /* static setSnackbar(color: any, message: any) {
        let snackbar = {
            show: false,
            color: '',
            message: ''
        };

        snackbar.show = true;
        snackbar.color = color;
        snackbar.message = message;

        return snackbar;
    } */
}